import { encode, decode } from "js-base64";
import CryptoJS from "crypto-js";

class OssUpload {
  config = {};
  setConfig(config) {
    this.config = config;
  }

  async upload(file) {

  }
}

export default OssUpload;

console.log(123)
